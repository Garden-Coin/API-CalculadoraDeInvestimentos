import { describe, it } from 'node:test';

describe(
    "validators",
    {
        concurrency: false,
        only: false,
        skip: false,
        timeout: Infinity,
        todo: true
    },
    ()=>{
        describe(
            "period",
            ()=>{
                it(
                    "should be bigger than zero",
                    ()=>{}
                )
            }
        )
    }
)